
version: '1'
services:

  chat:
    container_name: chat
    build: .
    expose:
      - 8080
    ports:
      - '8080:8080'
    # volumes:
    #   - /data/example/mysql:/var/lib/mysql
    restart: always


